We are working on Unit Testing in a React App !

- Install dependencies
    ```bash
    npm install
    ```
- Run the app
    ```bash
    npm run dev
    ```
- Write the tests
- Run them
    ```bash
    npm run test
    ```
